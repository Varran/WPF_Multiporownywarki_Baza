<Window x:Class="WPF_multi_próby.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:mx="clr-namespace:MatrixLib.Matrix;assembly=MatrixLib" 
        xmlns:local="clr-namespace:WPF_multi_próby"
        mc:Ignorable="d"
        Title="MainWindow" Height="700" Width="950">
    <Window.DataContext>
        <local:ViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <local:MatrixToDataViewConverter x:Key="MatrixToDataViewConverter"/>
    </Window.Resources>



    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="420"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="300"/>
            <RowDefinition Height="330"/>
        </Grid.RowDefinitions>
        <StackPanel Name="ListOfMixedPaint" 
                    Grid.Row="0" Grid.Column="1" Orientation="Vertical">
            <TextBlock Text="List of MixedPaint:" Margin="15,0,0,0"/>
            <ListView ItemsSource="{Binding MixedPaints}" SelectedItem="{Binding SelectedMixedPaint}" Margin="10">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="PaintName: "/>
                            <TextBlock Text="{Binding PaintName}" Width="120" FontWeight="Bold"/>
                            <TextBlock Text="IngradientCount: " Margin="0,0,10,0"/>
                            <TextBlock Text="{Binding Ingredients.Count}" FontWeight="Bold"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <TextBlock Text="Adding new MixedPaint" Margin="15,10,10,10"/>
            
        </StackPanel>
        <StackPanel Grid.Row="0" Grid.Column="2">
            <TextBlock Text="Ingredients of selected MixedPaint" Margin="15,0,0,0" TextWrapping="Wrap"/>
            <ListView ItemsSource="{Binding SelectedMixedPaint.Ingredients}" Margin="10">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="80"/>
                        <GridViewColumn Header="Saturation" DisplayMemberBinding="{Binding Saturation}" Width="60"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </StackPanel>
        <StackPanel Name="CreatAndAdd" Grid.Column="0" Grid.Row="0" Orientation="Vertical">
            <StackPanel Name="AddingMixedPaint" Orientation="Vertical">
                <TextBlock Text="Adding new Base Color" Margin="15,10,2,10" FontWeight="Bold"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Color Name:" Grid.Column="0" Grid.Row="0"/>
                    <TextBox Grid.Row="0" Grid.Column="1"/>
                    <TextBlock Text="Color Saturation:" Grid.Column="0" Grid.Row="1"/>
                    <TextBox Grid.Row="1" Grid.Column="1"/>
                    <Button Content="Add New Color" Grid.Row="2" Grid.Column="1" Margin="100,0,10,0"/>
                </Grid>
                <TextBlock Text="All base colors" Margin="15,0,0,0" FontWeight="Bold"/>
                <ListView ItemsSource="{Binding Basecolors}" Margin="10">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Color Name" DisplayMemberBinding="{Binding Name}" Width="100"/>
                            <GridViewColumn Header="Color Saturation" DisplayMemberBinding="{Binding Saturation}" Width="100"/>
                        </GridView>
                    </ListView.View>
                </ListView>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="1" Content="Add selected Color to new Mixed >>" Margin="30,0,10,0"/>
                </Grid>
            </StackPanel>
            <StackPanel Name="AddingBseColor" Orientation="Vertical">

            </StackPanel>
        </StackPanel>


        <!--https://stackoverflow.com/questions/28018974/binding-matrix-arrays-to-wpf-datagrid-->
        <StackPanel Grid.Column="0" Grid.Row="1" Orientation="Vertical">
            <TextBlock Text="DataMatrix Stackoverflow" Margin="15,0,0,0"/>
            <DataGrid   CanUserAddRows="False" Margin="10" >
                <DataGrid.ItemsSource>
                    <MultiBinding Converter="{StaticResource MatrixToDataViewConverter}">
                        <Binding Path="ColumnHeaders"/>
                        <Binding Path="RowHeaders"/>
                        <Binding Path="Values"/>
                    </MultiBinding>
                </DataGrid.ItemsSource>
            </DataGrid>
        </StackPanel>


        <!--https://www.codeproject.com/Articles/37241/Displaying-a-Data-Matrix-in-WPF-->
        <StackPanel Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="1">
            <TextBlock Text="DataMatrix CodeProject" Margin="15,0,0,0"/>
            <Grid >
                <mx:MatrixControl ItemsSource="{Binding Path=MatrixItems}" Margin="10" BorderBrush="Black" BorderThickness="1">
                    <mx:MatrixControl.DataContext>
                        <local:ViewModel />
                    </mx:MatrixControl.DataContext>
                    <mx:MatrixControl.Resources>
                        <ResourceDictionary Source="PaintMatrixTemplates.xaml" />
                    </mx:MatrixControl.Resources>
                </mx:MatrixControl>
            </Grid>
        </StackPanel>

        <!--<StackPanel Name="MultiComparerOfPaints"
                    Grid.Column="0" Grid.Row="1"  Orientation="Vertical">
            <TextBlock Text="Multicomparer of paints" Margin="15,0,0,0"/>
            <ListView ItemsSource="{Binding ComparisonMatrix}" Margin="10" FontFamily="Cascadia Code" >
                <ListView.Resources>
                    <DataTemplate>
                        <CheckBox IsChecked="{Binding Matrix.Values}"/>
                    </DataTemplate>
                </ListView.Resources>
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Unique ingredient" DisplayMemberBinding="{Binding ColorIngredient}" Width="200"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </StackPanel>-->
    </Grid>
</Window>
